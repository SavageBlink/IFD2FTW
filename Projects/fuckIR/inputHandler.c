#include <inputHandler.h>
/*
int irctoi(char* irc)
{
    int c_irc = atoi(irc);
    for(int i = 0; i<13;i++)
    {
        if(c_irc == IRCs[i])
        {
            return IRCs[i];
        }
        
    }
    return -1;
}

boolean checkCode(int userC,int inputC[4])
{
    int m = pow(10,3) * inputC[0];
    int c = pow(10,3) * inputC[1];
    int d = pow(10,3) * inputC[2];
    int u = pow(10,3) * inputC[3];

    int tot = m+c+d+u;
    if(userC == tot)
    {
        return True;
    }
    else
    {
        return False;
    }
    
}*/